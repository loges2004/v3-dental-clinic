# ==========================
# Server Configuration
# ==========================
server.port=${PORT:8001}
server.address=0.0.0.0

# ==========================
# Database Configuration
# ==========================
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/v3dentalclinic?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ZPQWJFZGFCQfFVrLlPUTZwuwSENFkDTI
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# Initialize the database using schema.sql
spring.sql.init.mode=never
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# ==========================
# JWT Configuration
# ==========================
jwt.secret=${JWT_SECRET:VqI2IsDkMVTMdXEwyqFjvEfy2a062btQDPnAj9VnRfA=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ==========================
# Email Configuration
# ==========================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:v3dentalclinic@gmail.com}
spring.mail.password=${MAIL_PASSWORD:erkd nyuq xjwh jovs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ==========================
# File Upload Configuration
# ==========================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration is now managed in SecurityConfig.java

# ==========================
# Production Settings
# ==========================
spring.profiles.active=production
logging.level.root=WARN
logging.level.com.dental=INFO