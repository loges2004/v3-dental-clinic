# ==========================
# Server Configuration
# ==========================
server.port=8001
server.address=0.0.0.0

# ==========================
# Database Configuration
# ==========================
spring.datasource.url=jdbc:mysql://v3dental-database-v3dentalclinic-d9e2.b.aivencloud.com:21538/defaultdb?useSSL=true&requireSSL=true&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
# Optional fallback if you set DB_PASSWORD in .env
# dotenv library loads backend/.env into environment before Spring reads properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# ==========================
# JWT Configuration
# ==========================
jwt.secret=VqI2IsDkMVTMdXEwyqFjvEfy2a062btQDPnAj9VnRfA=
jwt.expiration=86400000

# ==========================
# Email Configuration
# ==========================
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=v3dentalclinic@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.smtp.ssl.required=true
logging.level.org.springframework.mail=DEBUG
logging.level.com.dental.service.EmailService=DEBUG
# ==========================
# File Upload Configuration
# ==========================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration is now managed in SecurityConfig.java

# ==========================
# Production Settings
# ==========================
spring.profiles.active=production
logging.level.root=WARN
logging.level.com.dental=INFO

# ==========================
# Git Ignore Configuration
# ==========================
backend/target/
